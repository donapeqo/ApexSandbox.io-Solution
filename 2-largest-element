public static Integer findLargest(List<Integer> nums) {
	//code here 
    Integer maxNum = nums[0];
    for(Integer i = 0; i<= nums.size()-1; i++){
    if(nums[i]> maxNum){
        maxNum = nums[i];
        
    
    } 
    system.debug('maxNum is '+ maxNum );
    }

    return maxNum;
}