public Boolean isPrime(Integer num) {
    //code here
    for(Integer i=2; i<num; i++){
        if(Math.mod(num,num[i]!= 0)){
            return true;
            break
        } else {
            return false;
        }
    }
}