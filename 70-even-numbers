public List<Integer> evenNumbers(Integer n) {
    //code here
    List<Integer> evenNum = new List<Integer>();
    Integer eve;
    for(Integer i = 2; i <= n; i++){

        eve = 2*i;

        evenNum.add(eve);
        
    }

    return evenNum;
}